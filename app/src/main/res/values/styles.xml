<resources>
    <!-- Header Days (SU, MO, TU...) -->
    <style name="CalendarDayHeader">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#94A3B8</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Active Dates in current month -->
    <style name="CalendarDateActive">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:layout_margin">2dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#0F172A</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Inactive Dates (Previous/Next month) -->
    <style name="CalendarDateInactive" parent="CalendarDateActive">
        <item name="android:textColor">#94A3B8</item>
    </style>
</resources>